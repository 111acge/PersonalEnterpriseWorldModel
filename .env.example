# 本地知识库 LLM API 配置（可选）
#
# 生效方式（任选其一）：
#   1. 推荐：在 GUI「API 配置」Tab 中填写（保存到 ~/.enterprise_world_model/config.json，跨 exe 保留）
#   2. 兜底：设置下面的 OPENAI_API_KEY 环境变量（所有提供商都用这一个变量名）
#
# 支持 DeepSeek / Kimi (Moonshot) / MiniMax，均走 OpenAI 兼容 API
OPENAI_API_KEY=sk-xxxxxxxx

# 向量索引配置（可选）
# 首次运行本体生成时会自动下载 bge-small-zh embedding 模型（~100MB）
# 向量库路径：data/vector/index.pkl

# OCR 配置（可选）
# 推荐在 GUI「OCR 配置」Tab 中选择 API 模式并填写百度/腾讯/阿里云的 Key
# 如选择本地模式，首次处理图片时会自动下载 PaddleOCR 中文模型（~150MB）
# 模型路径：data/ocr_models/
